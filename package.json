{
  "name": "yjs-server",
  "version": "1.0.0",
  "description": "y-websocket server with frontend for real-time collaboration",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.22.1",
    "ws": "^8.18.0",
    "y-websocket": "^1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
